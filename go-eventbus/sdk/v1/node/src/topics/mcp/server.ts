// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v6.33.6
// source: mcp/server.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "lightrag.eventbus.topics.v1";

export enum ServerState {
  SERVER_STATE_UNSPECIFIED = 0,
  /** SERVER_STATE_STOPPED - 已停止 */
  SERVER_STATE_STOPPED = 1,
  /** SERVER_STATE_STARTING - 启动中 */
  SERVER_STATE_STARTING = 2,
  /** SERVER_STATE_RUNNING - 运行中 */
  SERVER_STATE_RUNNING = 3,
  /** SERVER_STATE_IDLE - 空闲（进程存活但无活跃连接） */
  SERVER_STATE_IDLE = 4,
  /** SERVER_STATE_STOPPING - 停止中 */
  SERVER_STATE_STOPPING = 5,
  /** SERVER_STATE_ERROR - 错误状态 */
  SERVER_STATE_ERROR = 6,
  UNRECOGNIZED = -1,
}

export enum ServerType {
  SERVER_TYPE_UNSPECIFIED = 0,
  /** SERVER_TYPE_STDIO - 本地进程，通过 stdin/stdout 通信 */
  SERVER_TYPE_STDIO = 1,
  /** SERVER_TYPE_HTTP - 远程 HTTP/SSE 服务 */
  SERVER_TYPE_HTTP = 2,
  UNRECOGNIZED = -1,
}

export enum RestartPolicy {
  RESTART_POLICY_UNSPECIFIED = 0,
  /** RESTART_POLICY_NEVER - 不自动重启 */
  RESTART_POLICY_NEVER = 1,
  /** RESTART_POLICY_ON_FAILURE - 失败时重启 */
  RESTART_POLICY_ON_FAILURE = 2,
  /** RESTART_POLICY_ALWAYS - 始终重启 */
  RESTART_POLICY_ALWAYS = 3,
  UNRECOGNIZED = -1,
}

export interface ServerConfig {
  /** 服务器名称（唯一标识） */
  name: string;
  /** 服务器类型 */
  type: ServerType;
  stdio?: StdioConfig | undefined;
  http?:
    | HttpConfig
    | undefined;
  /** 重启策略 */
  restartPolicy: RestartPolicy;
  /** 最大重试次数 */
  maxRetries: number;
  /** 重试延迟（毫秒） */
  retryDelayMs: number;
  /** 启动超时（毫秒） */
  startupTimeoutMs: number;
  /** 请求超时（毫秒） */
  requestTimeoutMs: number;
  /** 空闲超时（毫秒） */
  idleTimeoutMs: number;
  /** 自定义标签 */
  labels: { [key: string]: string };
}

export interface ServerConfig_LabelsEntry {
  key: string;
  value: string;
}

export interface StdioConfig {
  /** 命令路径 */
  command: string;
  /** 命令参数 */
  args: string[];
  /** 环境变量 */
  env: { [key: string]: string };
  /** 工作目录 */
  workingDir: string;
}

export interface StdioConfig_EnvEntry {
  key: string;
  value: string;
}

export interface HttpConfig {
  /** 服务 URL */
  url: string;
  /** 自定义请求头 */
  headers: { [key: string]: string };
  /** 是否启用 SSE */
  sseEnabled: boolean;
}

export interface HttpConfig_HeadersEntry {
  key: string;
  value: string;
}

export interface ServerStatus {
  /** 服务器名称 */
  name: string;
  /** 服务器类型 */
  type: ServerType;
  /** 当前状态 */
  state: ServerState;
  /** 进程 ID（stdio 类型） */
  pid: number;
  /** 工具数量 */
  toolCount: number;
  /** 创建时间 */
  createdAt: number;
  /** 启动时间 */
  startedAt: number;
  /** 最后活跃时间 */
  lastActiveAt: number;
  /** 错误信息 */
  errorMessage: string;
  /** 重启次数 */
  restartCount: number;
  labels: { [key: string]: string };
}

export interface ServerStatus_LabelsEntry {
  key: string;
  value: string;
}

export interface ToolInfo {
  /** 工具名称 */
  name: string;
  /** 工具描述 */
  description: string;
  /** 输入参数 JSON Schema */
  inputSchema: string;
  /** 所属服务器名称 */
  serverName: string;
  /** 完整名称 (server.tool) */
  fullName: string;
}

export interface ToolCallRequest {
  /** 服务器名称 */
  serverName: string;
  /** 工具名称 */
  toolName: string;
  /** 工具参数（JSON 编码） */
  arguments: { [key: string]: Uint8Array };
  /** 调用超时 */
  timeoutMs: number;
}

export interface ToolCallRequest_ArgumentsEntry {
  key: string;
  value: Uint8Array;
}

export interface ToolCallResponse {
  /** 是否错误 */
  isError: boolean;
  /** 返回内容 */
  content: ToolContent[];
  /** 执行耗时 */
  durationMs: number;
}

export interface ToolContent {
  /** 内容类型 (text, image, resource) */
  type: string;
  /** 内容数据 */
  data: Uint8Array;
  /** MIME 类型 */
  mimeType: string;
}

export interface CreateServerInput {
  /** 服务器配置 */
  config: ServerConfig | undefined;
}

export interface CreateServerOutput {
  /** 创建后的状态 */
  status: ServerStatus | undefined;
}

export interface GetServerInput {
  /** 服务器名称 */
  name: string;
}

export interface GetServerOutput {
  status: ServerStatus | undefined;
}

export interface ListServersInput {
  /** 是否包含已停止的服务器 */
  includeStopped: boolean;
  /** 标签过滤 */
  labelFilter: { [key: string]: string };
}

export interface ListServersInput_LabelFilterEntry {
  key: string;
  value: string;
}

export interface ListServersOutput {
  servers: ServerStatus[];
  totalCount: number;
}

export interface UpdateServerInput {
  /** 新的服务器配置 */
  config:
    | ServerConfig
    | undefined;
  /** 是否更新运行中的服务器 */
  updateRunning: boolean;
}

export interface UpdateServerOutput {
  status:
    | ServerStatus
    | undefined;
  /** 是否需要重启生效 */
  requiresRestart: boolean;
}

export interface DeleteServerInput {
  /** 服务器名称 */
  name: string;
  /** 强制删除（包括停止运行中的服务器） */
  force: boolean;
}

export interface DeleteServerOutput {
  success: boolean;
}

export interface StartServerInput {
  /** 服务器名称 */
  name: string;
  /** 启动超时 */
  timeoutMs: number;
  /** 懒加载（启动后立即进入空闲） */
  lazy: boolean;
}

export interface StartServerOutput {
  status: ServerStatus | undefined;
}

export interface StopServerInput {
  /** 服务器名称 */
  name: string;
  /** 优雅停止 */
  graceful: boolean;
  /** 停止超时 */
  timeoutMs: number;
}

export interface StopServerOutput {
  status: ServerStatus | undefined;
}

export interface RestartServerInput {
  /** 服务器名称 */
  name: string;
  /** 重启超时 */
  timeoutMs: number;
}

export interface RestartServerOutput {
  status: ServerStatus | undefined;
}

export interface GetServerStatusInput {
  /** 服务器名称 */
  name: string;
  /** 是否包含工具列表 */
  includeTools: boolean;
}

export interface GetServerStatusOutput {
  status:
    | ServerStatus
    | undefined;
  /** 工具列表（可选） */
  tools: ToolInfo[];
}

export interface ListToolsInput {
  /** 服务器名称 */
  serverName: string;
  /** 工具名称过滤 */
  filter: string;
}

export interface ListToolsOutput {
  tools: ToolInfo[];
  totalCount: number;
}

export interface SearchResult {
  /** 文档 ID */
  documentId: string;
  /** 匹配的文本内容 */
  content: string;
  /** 匹配分数 */
  score: number;
  /** 结构化字段 */
  fields: { [key: string]: string };
}

export interface SearchResult_FieldsEntry {
  key: string;
  value: string;
}

export interface SearchToolsInput {
  /** 搜索查询 */
  query: string;
  /** 索引名称 (default: "mcp_tools") */
  indexName: string;
  /** 返回数量 (default: 10) */
  topK: number;
  /** 语义比例 0=关键词, 1=向量 (default: 0.7) */
  semanticRatio: number;
  /** 最低分数阈值 (default: 0.5) */
  scoreThreshold: number;
  /** 嵌入器名称 (default: "default") */
  embedder: string;
}

export interface SearchToolsOutput {
  /** 搜索结果 */
  results: SearchResult[];
  /** 总命中数 */
  totalHits: number;
  /** 回显索引名称 */
  indexName: string;
}

export interface ToolIndexDocument {
  /** 文档 ID */
  documentId: string;
  /** 用于搜索的文本内容 */
  content: string;
  /** 结构化字段 (name, description, server, full_name) */
  fields: { [key: string]: string };
}

export interface ToolIndexDocument_FieldsEntry {
  key: string;
  value: string;
}

export interface IndexToolsInput {
  /** 索引名称 (default: "mcp_tools") */
  indexName: string;
  /** 待索引的文档列表 */
  documents: ToolIndexDocument[];
  /** "upsert" 或 "delete" */
  action: string;
  /** 是否生成向量 */
  enableVector: boolean;
  /** 嵌入器名称 (default: "default") */
  embedder: string;
}

export interface IndexToolsOutput {
  /** 成功索引数量 */
  indexedCount: number;
  /** 已索引的文档 ID */
  indexed: string[];
  /** 失败的文档 ID */
  failed: string[];
}

export interface CallToolInput {
  /** 工具调用请求 */
  request: ToolCallRequest | undefined;
}

export interface CallToolOutput {
  /** 工具调用响应 */
  response: ToolCallResponse | undefined;
}

export interface BatchStartInput {
  /** 服务器名称列表 */
  names: string[];
  timeoutMs: number;
  /** 是否并行启动 */
  parallel: boolean;
}

export interface BatchStartOutput {
  /** 每个服务器的启动结果 */
  results: { [key: string]: ServerStatus };
  /** 失败的服务器列表 */
  failed: string[];
}

export interface BatchStartOutput_ResultsEntry {
  key: string;
  value: ServerStatus | undefined;
}

export interface BatchStopInput {
  /** 服务器名称列表 */
  names: string[];
  /** 优雅停止 */
  graceful: boolean;
}

export interface BatchStopOutput {
  /** 每个服务器的停止结果 */
  results: { [key: string]: ServerStatus };
  /** 失败的服务器列表 */
  failed: string[];
}

export interface BatchStopOutput_ResultsEntry {
  key: string;
  value: ServerStatus | undefined;
}

export interface ServerEventInput {
  /** created, updated, deleted, started, stopped, error */
  eventType: string;
  status:
    | ServerStatus
    | undefined;
  /** 事件描述 */
  message: string;
  /** 事件时间戳 */
  timestamp: number;
}

export interface ServerEventOutput {
  /** 事件是否被接收 */
  acknowledged: boolean;
}

function createBaseServerConfig(): ServerConfig {
  return {
    name: "",
    type: 0,
    stdio: undefined,
    http: undefined,
    restartPolicy: 0,
    maxRetries: 0,
    retryDelayMs: 0,
    startupTimeoutMs: 0,
    requestTimeoutMs: 0,
    idleTimeoutMs: 0,
    labels: {},
  };
}

export const ServerConfig: MessageFns<ServerConfig> = {
  encode(message: ServerConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    if (message.stdio !== undefined) {
      StdioConfig.encode(message.stdio, writer.uint32(26).fork()).join();
    }
    if (message.http !== undefined) {
      HttpConfig.encode(message.http, writer.uint32(34).fork()).join();
    }
    if (message.restartPolicy !== 0) {
      writer.uint32(80).int32(message.restartPolicy);
    }
    if (message.maxRetries !== 0) {
      writer.uint32(88).int32(message.maxRetries);
    }
    if (message.retryDelayMs !== 0) {
      writer.uint32(96).int32(message.retryDelayMs);
    }
    if (message.startupTimeoutMs !== 0) {
      writer.uint32(160).int32(message.startupTimeoutMs);
    }
    if (message.requestTimeoutMs !== 0) {
      writer.uint32(168).int32(message.requestTimeoutMs);
    }
    if (message.idleTimeoutMs !== 0) {
      writer.uint32(176).int32(message.idleTimeoutMs);
    }
    globalThis.Object.entries(message.labels).forEach(([key, value]: [string, string]) => {
      ServerConfig_LabelsEntry.encode({ key: key as any, value }, writer.uint32(242).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.stdio = StdioConfig.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.http = HttpConfig.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.restartPolicy = reader.int32() as any;
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.maxRetries = reader.int32();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.retryDelayMs = reader.int32();
          continue;
        }
        case 20: {
          if (tag !== 160) {
            break;
          }

          message.startupTimeoutMs = reader.int32();
          continue;
        }
        case 21: {
          if (tag !== 168) {
            break;
          }

          message.requestTimeoutMs = reader.int32();
          continue;
        }
        case 22: {
          if (tag !== 176) {
            break;
          }

          message.idleTimeoutMs = reader.int32();
          continue;
        }
        case 30: {
          if (tag !== 242) {
            break;
          }

          const entry30 = ServerConfig_LabelsEntry.decode(reader, reader.uint32());
          if (entry30.value !== undefined) {
            message.labels[entry30.key] = entry30.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ServerConfig>, I>>(base?: I): ServerConfig {
    return ServerConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerConfig>, I>>(object: I): ServerConfig {
    const message = createBaseServerConfig();
    message.name = object.name ?? "";
    message.type = object.type ?? 0;
    message.stdio = (object.stdio !== undefined && object.stdio !== null)
      ? StdioConfig.fromPartial(object.stdio)
      : undefined;
    message.http = (object.http !== undefined && object.http !== null)
      ? HttpConfig.fromPartial(object.http)
      : undefined;
    message.restartPolicy = object.restartPolicy ?? 0;
    message.maxRetries = object.maxRetries ?? 0;
    message.retryDelayMs = object.retryDelayMs ?? 0;
    message.startupTimeoutMs = object.startupTimeoutMs ?? 0;
    message.requestTimeoutMs = object.requestTimeoutMs ?? 0;
    message.idleTimeoutMs = object.idleTimeoutMs ?? 0;
    message.labels = (globalThis.Object.entries(object.labels ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseServerConfig_LabelsEntry(): ServerConfig_LabelsEntry {
  return { key: "", value: "" };
}

export const ServerConfig_LabelsEntry: MessageFns<ServerConfig_LabelsEntry> = {
  encode(message: ServerConfig_LabelsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerConfig_LabelsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerConfig_LabelsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ServerConfig_LabelsEntry>, I>>(base?: I): ServerConfig_LabelsEntry {
    return ServerConfig_LabelsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerConfig_LabelsEntry>, I>>(object: I): ServerConfig_LabelsEntry {
    const message = createBaseServerConfig_LabelsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseStdioConfig(): StdioConfig {
  return { command: "", args: [], env: {}, workingDir: "" };
}

export const StdioConfig: MessageFns<StdioConfig> = {
  encode(message: StdioConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.command !== "") {
      writer.uint32(10).string(message.command);
    }
    for (const v of message.args) {
      writer.uint32(18).string(v!);
    }
    globalThis.Object.entries(message.env).forEach(([key, value]: [string, string]) => {
      StdioConfig_EnvEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    if (message.workingDir !== "") {
      writer.uint32(34).string(message.workingDir);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StdioConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStdioConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.command = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.args.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = StdioConfig_EnvEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.env[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.workingDir = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<StdioConfig>, I>>(base?: I): StdioConfig {
    return StdioConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StdioConfig>, I>>(object: I): StdioConfig {
    const message = createBaseStdioConfig();
    message.command = object.command ?? "";
    message.args = object.args?.map((e) => e) || [];
    message.env = (globalThis.Object.entries(object.env ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.workingDir = object.workingDir ?? "";
    return message;
  },
};

function createBaseStdioConfig_EnvEntry(): StdioConfig_EnvEntry {
  return { key: "", value: "" };
}

export const StdioConfig_EnvEntry: MessageFns<StdioConfig_EnvEntry> = {
  encode(message: StdioConfig_EnvEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StdioConfig_EnvEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStdioConfig_EnvEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<StdioConfig_EnvEntry>, I>>(base?: I): StdioConfig_EnvEntry {
    return StdioConfig_EnvEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StdioConfig_EnvEntry>, I>>(object: I): StdioConfig_EnvEntry {
    const message = createBaseStdioConfig_EnvEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseHttpConfig(): HttpConfig {
  return { url: "", headers: {}, sseEnabled: false };
}

export const HttpConfig: MessageFns<HttpConfig> = {
  encode(message: HttpConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    globalThis.Object.entries(message.headers).forEach(([key, value]: [string, string]) => {
      HttpConfig_HeadersEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    if (message.sseEnabled !== false) {
      writer.uint32(24).bool(message.sseEnabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = HttpConfig_HeadersEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.headers[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sseEnabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<HttpConfig>, I>>(base?: I): HttpConfig {
    return HttpConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HttpConfig>, I>>(object: I): HttpConfig {
    const message = createBaseHttpConfig();
    message.url = object.url ?? "";
    message.headers = (globalThis.Object.entries(object.headers ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.sseEnabled = object.sseEnabled ?? false;
    return message;
  },
};

function createBaseHttpConfig_HeadersEntry(): HttpConfig_HeadersEntry {
  return { key: "", value: "" };
}

export const HttpConfig_HeadersEntry: MessageFns<HttpConfig_HeadersEntry> = {
  encode(message: HttpConfig_HeadersEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpConfig_HeadersEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpConfig_HeadersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<HttpConfig_HeadersEntry>, I>>(base?: I): HttpConfig_HeadersEntry {
    return HttpConfig_HeadersEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HttpConfig_HeadersEntry>, I>>(object: I): HttpConfig_HeadersEntry {
    const message = createBaseHttpConfig_HeadersEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseServerStatus(): ServerStatus {
  return {
    name: "",
    type: 0,
    state: 0,
    pid: 0,
    toolCount: 0,
    createdAt: 0,
    startedAt: 0,
    lastActiveAt: 0,
    errorMessage: "",
    restartCount: 0,
    labels: {},
  };
}

export const ServerStatus: MessageFns<ServerStatus> = {
  encode(message: ServerStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    if (message.state !== 0) {
      writer.uint32(24).int32(message.state);
    }
    if (message.pid !== 0) {
      writer.uint32(32).int64(message.pid);
    }
    if (message.toolCount !== 0) {
      writer.uint32(40).int32(message.toolCount);
    }
    if (message.createdAt !== 0) {
      writer.uint32(80).int64(message.createdAt);
    }
    if (message.startedAt !== 0) {
      writer.uint32(88).int64(message.startedAt);
    }
    if (message.lastActiveAt !== 0) {
      writer.uint32(96).int64(message.lastActiveAt);
    }
    if (message.errorMessage !== "") {
      writer.uint32(162).string(message.errorMessage);
    }
    if (message.restartCount !== 0) {
      writer.uint32(168).int32(message.restartCount);
    }
    globalThis.Object.entries(message.labels).forEach(([key, value]: [string, string]) => {
      ServerStatus_LabelsEntry.encode({ key: key as any, value }, writer.uint32(242).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.pid = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.toolCount = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.createdAt = longToNumber(reader.int64());
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.startedAt = longToNumber(reader.int64());
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.lastActiveAt = longToNumber(reader.int64());
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 21: {
          if (tag !== 168) {
            break;
          }

          message.restartCount = reader.int32();
          continue;
        }
        case 30: {
          if (tag !== 242) {
            break;
          }

          const entry30 = ServerStatus_LabelsEntry.decode(reader, reader.uint32());
          if (entry30.value !== undefined) {
            message.labels[entry30.key] = entry30.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ServerStatus>, I>>(base?: I): ServerStatus {
    return ServerStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerStatus>, I>>(object: I): ServerStatus {
    const message = createBaseServerStatus();
    message.name = object.name ?? "";
    message.type = object.type ?? 0;
    message.state = object.state ?? 0;
    message.pid = object.pid ?? 0;
    message.toolCount = object.toolCount ?? 0;
    message.createdAt = object.createdAt ?? 0;
    message.startedAt = object.startedAt ?? 0;
    message.lastActiveAt = object.lastActiveAt ?? 0;
    message.errorMessage = object.errorMessage ?? "";
    message.restartCount = object.restartCount ?? 0;
    message.labels = (globalThis.Object.entries(object.labels ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseServerStatus_LabelsEntry(): ServerStatus_LabelsEntry {
  return { key: "", value: "" };
}

export const ServerStatus_LabelsEntry: MessageFns<ServerStatus_LabelsEntry> = {
  encode(message: ServerStatus_LabelsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerStatus_LabelsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerStatus_LabelsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ServerStatus_LabelsEntry>, I>>(base?: I): ServerStatus_LabelsEntry {
    return ServerStatus_LabelsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerStatus_LabelsEntry>, I>>(object: I): ServerStatus_LabelsEntry {
    const message = createBaseServerStatus_LabelsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseToolInfo(): ToolInfo {
  return { name: "", description: "", inputSchema: "", serverName: "", fullName: "" };
}

export const ToolInfo: MessageFns<ToolInfo> = {
  encode(message: ToolInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.inputSchema !== "") {
      writer.uint32(26).string(message.inputSchema);
    }
    if (message.serverName !== "") {
      writer.uint32(34).string(message.serverName);
    }
    if (message.fullName !== "") {
      writer.uint32(42).string(message.fullName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.inputSchema = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.serverName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.fullName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ToolInfo>, I>>(base?: I): ToolInfo {
    return ToolInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolInfo>, I>>(object: I): ToolInfo {
    const message = createBaseToolInfo();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.inputSchema = object.inputSchema ?? "";
    message.serverName = object.serverName ?? "";
    message.fullName = object.fullName ?? "";
    return message;
  },
};

function createBaseToolCallRequest(): ToolCallRequest {
  return { serverName: "", toolName: "", arguments: {}, timeoutMs: 0 };
}

export const ToolCallRequest: MessageFns<ToolCallRequest> = {
  encode(message: ToolCallRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverName !== "") {
      writer.uint32(10).string(message.serverName);
    }
    if (message.toolName !== "") {
      writer.uint32(18).string(message.toolName);
    }
    globalThis.Object.entries(message.arguments).forEach(([key, value]: [string, Uint8Array]) => {
      ToolCallRequest_ArgumentsEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    if (message.timeoutMs !== 0) {
      writer.uint32(32).int32(message.timeoutMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolCallRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolCallRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.toolName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = ToolCallRequest_ArgumentsEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.arguments[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.timeoutMs = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ToolCallRequest>, I>>(base?: I): ToolCallRequest {
    return ToolCallRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolCallRequest>, I>>(object: I): ToolCallRequest {
    const message = createBaseToolCallRequest();
    message.serverName = object.serverName ?? "";
    message.toolName = object.toolName ?? "";
    message.arguments = (globalThis.Object.entries(object.arguments ?? {}) as [string, Uint8Array][]).reduce(
      (acc: { [key: string]: Uint8Array }, [key, value]: [string, Uint8Array]) => {
        if (value !== undefined) {
          acc[key] = value;
        }
        return acc;
      },
      {},
    );
    message.timeoutMs = object.timeoutMs ?? 0;
    return message;
  },
};

function createBaseToolCallRequest_ArgumentsEntry(): ToolCallRequest_ArgumentsEntry {
  return { key: "", value: new Uint8Array(0) };
}

export const ToolCallRequest_ArgumentsEntry: MessageFns<ToolCallRequest_ArgumentsEntry> = {
  encode(message: ToolCallRequest_ArgumentsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolCallRequest_ArgumentsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolCallRequest_ArgumentsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ToolCallRequest_ArgumentsEntry>, I>>(base?: I): ToolCallRequest_ArgumentsEntry {
    return ToolCallRequest_ArgumentsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolCallRequest_ArgumentsEntry>, I>>(
    object: I,
  ): ToolCallRequest_ArgumentsEntry {
    const message = createBaseToolCallRequest_ArgumentsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? new Uint8Array(0);
    return message;
  },
};

function createBaseToolCallResponse(): ToolCallResponse {
  return { isError: false, content: [], durationMs: 0 };
}

export const ToolCallResponse: MessageFns<ToolCallResponse> = {
  encode(message: ToolCallResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isError !== false) {
      writer.uint32(8).bool(message.isError);
    }
    for (const v of message.content) {
      ToolContent.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.durationMs !== 0) {
      writer.uint32(24).int64(message.durationMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolCallResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolCallResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.isError = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content.push(ToolContent.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.durationMs = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ToolCallResponse>, I>>(base?: I): ToolCallResponse {
    return ToolCallResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolCallResponse>, I>>(object: I): ToolCallResponse {
    const message = createBaseToolCallResponse();
    message.isError = object.isError ?? false;
    message.content = object.content?.map((e) => ToolContent.fromPartial(e)) || [];
    message.durationMs = object.durationMs ?? 0;
    return message;
  },
};

function createBaseToolContent(): ToolContent {
  return { type: "", data: new Uint8Array(0), mimeType: "" };
}

export const ToolContent: MessageFns<ToolContent> = {
  encode(message: ToolContent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== "") {
      writer.uint32(10).string(message.type);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    if (message.mimeType !== "") {
      writer.uint32(26).string(message.mimeType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolContent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ToolContent>, I>>(base?: I): ToolContent {
    return ToolContent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolContent>, I>>(object: I): ToolContent {
    const message = createBaseToolContent();
    message.type = object.type ?? "";
    message.data = object.data ?? new Uint8Array(0);
    message.mimeType = object.mimeType ?? "";
    return message;
  },
};

function createBaseCreateServerInput(): CreateServerInput {
  return { config: undefined };
}

export const CreateServerInput: MessageFns<CreateServerInput> = {
  encode(message: CreateServerInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.config !== undefined) {
      ServerConfig.encode(message.config, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateServerInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateServerInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.config = ServerConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CreateServerInput>, I>>(base?: I): CreateServerInput {
    return CreateServerInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateServerInput>, I>>(object: I): CreateServerInput {
    const message = createBaseCreateServerInput();
    message.config = (object.config !== undefined && object.config !== null)
      ? ServerConfig.fromPartial(object.config)
      : undefined;
    return message;
  },
};

function createBaseCreateServerOutput(): CreateServerOutput {
  return { status: undefined };
}

export const CreateServerOutput: MessageFns<CreateServerOutput> = {
  encode(message: CreateServerOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== undefined) {
      ServerStatus.encode(message.status, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateServerOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateServerOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = ServerStatus.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CreateServerOutput>, I>>(base?: I): CreateServerOutput {
    return CreateServerOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateServerOutput>, I>>(object: I): CreateServerOutput {
    const message = createBaseCreateServerOutput();
    message.status = (object.status !== undefined && object.status !== null)
      ? ServerStatus.fromPartial(object.status)
      : undefined;
    return message;
  },
};

function createBaseGetServerInput(): GetServerInput {
  return { name: "" };
}

export const GetServerInput: MessageFns<GetServerInput> = {
  encode(message: GetServerInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServerInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServerInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<GetServerInput>, I>>(base?: I): GetServerInput {
    return GetServerInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServerInput>, I>>(object: I): GetServerInput {
    const message = createBaseGetServerInput();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseGetServerOutput(): GetServerOutput {
  return { status: undefined };
}

export const GetServerOutput: MessageFns<GetServerOutput> = {
  encode(message: GetServerOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== undefined) {
      ServerStatus.encode(message.status, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServerOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServerOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = ServerStatus.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<GetServerOutput>, I>>(base?: I): GetServerOutput {
    return GetServerOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServerOutput>, I>>(object: I): GetServerOutput {
    const message = createBaseGetServerOutput();
    message.status = (object.status !== undefined && object.status !== null)
      ? ServerStatus.fromPartial(object.status)
      : undefined;
    return message;
  },
};

function createBaseListServersInput(): ListServersInput {
  return { includeStopped: false, labelFilter: {} };
}

export const ListServersInput: MessageFns<ListServersInput> = {
  encode(message: ListServersInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.includeStopped !== false) {
      writer.uint32(8).bool(message.includeStopped);
    }
    globalThis.Object.entries(message.labelFilter).forEach(([key, value]: [string, string]) => {
      ListServersInput_LabelFilterEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListServersInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListServersInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.includeStopped = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = ListServersInput_LabelFilterEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.labelFilter[entry2.key] = entry2.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ListServersInput>, I>>(base?: I): ListServersInput {
    return ListServersInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListServersInput>, I>>(object: I): ListServersInput {
    const message = createBaseListServersInput();
    message.includeStopped = object.includeStopped ?? false;
    message.labelFilter = (globalThis.Object.entries(object.labelFilter ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseListServersInput_LabelFilterEntry(): ListServersInput_LabelFilterEntry {
  return { key: "", value: "" };
}

export const ListServersInput_LabelFilterEntry: MessageFns<ListServersInput_LabelFilterEntry> = {
  encode(message: ListServersInput_LabelFilterEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListServersInput_LabelFilterEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListServersInput_LabelFilterEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ListServersInput_LabelFilterEntry>, I>>(
    base?: I,
  ): ListServersInput_LabelFilterEntry {
    return ListServersInput_LabelFilterEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListServersInput_LabelFilterEntry>, I>>(
    object: I,
  ): ListServersInput_LabelFilterEntry {
    const message = createBaseListServersInput_LabelFilterEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseListServersOutput(): ListServersOutput {
  return { servers: [], totalCount: 0 };
}

export const ListServersOutput: MessageFns<ListServersOutput> = {
  encode(message: ListServersOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.servers) {
      ServerStatus.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.totalCount !== 0) {
      writer.uint32(16).int32(message.totalCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListServersOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListServersOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.servers.push(ServerStatus.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ListServersOutput>, I>>(base?: I): ListServersOutput {
    return ListServersOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListServersOutput>, I>>(object: I): ListServersOutput {
    const message = createBaseListServersOutput();
    message.servers = object.servers?.map((e) => ServerStatus.fromPartial(e)) || [];
    message.totalCount = object.totalCount ?? 0;
    return message;
  },
};

function createBaseUpdateServerInput(): UpdateServerInput {
  return { config: undefined, updateRunning: false };
}

export const UpdateServerInput: MessageFns<UpdateServerInput> = {
  encode(message: UpdateServerInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.config !== undefined) {
      ServerConfig.encode(message.config, writer.uint32(10).fork()).join();
    }
    if (message.updateRunning !== false) {
      writer.uint32(16).bool(message.updateRunning);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateServerInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateServerInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.config = ServerConfig.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.updateRunning = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<UpdateServerInput>, I>>(base?: I): UpdateServerInput {
    return UpdateServerInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateServerInput>, I>>(object: I): UpdateServerInput {
    const message = createBaseUpdateServerInput();
    message.config = (object.config !== undefined && object.config !== null)
      ? ServerConfig.fromPartial(object.config)
      : undefined;
    message.updateRunning = object.updateRunning ?? false;
    return message;
  },
};

function createBaseUpdateServerOutput(): UpdateServerOutput {
  return { status: undefined, requiresRestart: false };
}

export const UpdateServerOutput: MessageFns<UpdateServerOutput> = {
  encode(message: UpdateServerOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== undefined) {
      ServerStatus.encode(message.status, writer.uint32(10).fork()).join();
    }
    if (message.requiresRestart !== false) {
      writer.uint32(16).bool(message.requiresRestart);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateServerOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateServerOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = ServerStatus.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.requiresRestart = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<UpdateServerOutput>, I>>(base?: I): UpdateServerOutput {
    return UpdateServerOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateServerOutput>, I>>(object: I): UpdateServerOutput {
    const message = createBaseUpdateServerOutput();
    message.status = (object.status !== undefined && object.status !== null)
      ? ServerStatus.fromPartial(object.status)
      : undefined;
    message.requiresRestart = object.requiresRestart ?? false;
    return message;
  },
};

function createBaseDeleteServerInput(): DeleteServerInput {
  return { name: "", force: false };
}

export const DeleteServerInput: MessageFns<DeleteServerInput> = {
  encode(message: DeleteServerInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.force !== false) {
      writer.uint32(16).bool(message.force);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteServerInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteServerInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.force = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<DeleteServerInput>, I>>(base?: I): DeleteServerInput {
    return DeleteServerInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteServerInput>, I>>(object: I): DeleteServerInput {
    const message = createBaseDeleteServerInput();
    message.name = object.name ?? "";
    message.force = object.force ?? false;
    return message;
  },
};

function createBaseDeleteServerOutput(): DeleteServerOutput {
  return { success: false };
}

export const DeleteServerOutput: MessageFns<DeleteServerOutput> = {
  encode(message: DeleteServerOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteServerOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteServerOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<DeleteServerOutput>, I>>(base?: I): DeleteServerOutput {
    return DeleteServerOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteServerOutput>, I>>(object: I): DeleteServerOutput {
    const message = createBaseDeleteServerOutput();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseStartServerInput(): StartServerInput {
  return { name: "", timeoutMs: 0, lazy: false };
}

export const StartServerInput: MessageFns<StartServerInput> = {
  encode(message: StartServerInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.timeoutMs !== 0) {
      writer.uint32(16).int32(message.timeoutMs);
    }
    if (message.lazy !== false) {
      writer.uint32(24).bool(message.lazy);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartServerInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartServerInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timeoutMs = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.lazy = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<StartServerInput>, I>>(base?: I): StartServerInput {
    return StartServerInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartServerInput>, I>>(object: I): StartServerInput {
    const message = createBaseStartServerInput();
    message.name = object.name ?? "";
    message.timeoutMs = object.timeoutMs ?? 0;
    message.lazy = object.lazy ?? false;
    return message;
  },
};

function createBaseStartServerOutput(): StartServerOutput {
  return { status: undefined };
}

export const StartServerOutput: MessageFns<StartServerOutput> = {
  encode(message: StartServerOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== undefined) {
      ServerStatus.encode(message.status, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartServerOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartServerOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = ServerStatus.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<StartServerOutput>, I>>(base?: I): StartServerOutput {
    return StartServerOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartServerOutput>, I>>(object: I): StartServerOutput {
    const message = createBaseStartServerOutput();
    message.status = (object.status !== undefined && object.status !== null)
      ? ServerStatus.fromPartial(object.status)
      : undefined;
    return message;
  },
};

function createBaseStopServerInput(): StopServerInput {
  return { name: "", graceful: false, timeoutMs: 0 };
}

export const StopServerInput: MessageFns<StopServerInput> = {
  encode(message: StopServerInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.graceful !== false) {
      writer.uint32(16).bool(message.graceful);
    }
    if (message.timeoutMs !== 0) {
      writer.uint32(24).int32(message.timeoutMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StopServerInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStopServerInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.graceful = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.timeoutMs = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<StopServerInput>, I>>(base?: I): StopServerInput {
    return StopServerInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StopServerInput>, I>>(object: I): StopServerInput {
    const message = createBaseStopServerInput();
    message.name = object.name ?? "";
    message.graceful = object.graceful ?? false;
    message.timeoutMs = object.timeoutMs ?? 0;
    return message;
  },
};

function createBaseStopServerOutput(): StopServerOutput {
  return { status: undefined };
}

export const StopServerOutput: MessageFns<StopServerOutput> = {
  encode(message: StopServerOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== undefined) {
      ServerStatus.encode(message.status, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StopServerOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStopServerOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = ServerStatus.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<StopServerOutput>, I>>(base?: I): StopServerOutput {
    return StopServerOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StopServerOutput>, I>>(object: I): StopServerOutput {
    const message = createBaseStopServerOutput();
    message.status = (object.status !== undefined && object.status !== null)
      ? ServerStatus.fromPartial(object.status)
      : undefined;
    return message;
  },
};

function createBaseRestartServerInput(): RestartServerInput {
  return { name: "", timeoutMs: 0 };
}

export const RestartServerInput: MessageFns<RestartServerInput> = {
  encode(message: RestartServerInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.timeoutMs !== 0) {
      writer.uint32(16).int32(message.timeoutMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RestartServerInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRestartServerInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timeoutMs = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RestartServerInput>, I>>(base?: I): RestartServerInput {
    return RestartServerInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RestartServerInput>, I>>(object: I): RestartServerInput {
    const message = createBaseRestartServerInput();
    message.name = object.name ?? "";
    message.timeoutMs = object.timeoutMs ?? 0;
    return message;
  },
};

function createBaseRestartServerOutput(): RestartServerOutput {
  return { status: undefined };
}

export const RestartServerOutput: MessageFns<RestartServerOutput> = {
  encode(message: RestartServerOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== undefined) {
      ServerStatus.encode(message.status, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RestartServerOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRestartServerOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = ServerStatus.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RestartServerOutput>, I>>(base?: I): RestartServerOutput {
    return RestartServerOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RestartServerOutput>, I>>(object: I): RestartServerOutput {
    const message = createBaseRestartServerOutput();
    message.status = (object.status !== undefined && object.status !== null)
      ? ServerStatus.fromPartial(object.status)
      : undefined;
    return message;
  },
};

function createBaseGetServerStatusInput(): GetServerStatusInput {
  return { name: "", includeTools: false };
}

export const GetServerStatusInput: MessageFns<GetServerStatusInput> = {
  encode(message: GetServerStatusInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.includeTools !== false) {
      writer.uint32(16).bool(message.includeTools);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServerStatusInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServerStatusInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.includeTools = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<GetServerStatusInput>, I>>(base?: I): GetServerStatusInput {
    return GetServerStatusInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServerStatusInput>, I>>(object: I): GetServerStatusInput {
    const message = createBaseGetServerStatusInput();
    message.name = object.name ?? "";
    message.includeTools = object.includeTools ?? false;
    return message;
  },
};

function createBaseGetServerStatusOutput(): GetServerStatusOutput {
  return { status: undefined, tools: [] };
}

export const GetServerStatusOutput: MessageFns<GetServerStatusOutput> = {
  encode(message: GetServerStatusOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== undefined) {
      ServerStatus.encode(message.status, writer.uint32(10).fork()).join();
    }
    for (const v of message.tools) {
      ToolInfo.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetServerStatusOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetServerStatusOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = ServerStatus.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tools.push(ToolInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<GetServerStatusOutput>, I>>(base?: I): GetServerStatusOutput {
    return GetServerStatusOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetServerStatusOutput>, I>>(object: I): GetServerStatusOutput {
    const message = createBaseGetServerStatusOutput();
    message.status = (object.status !== undefined && object.status !== null)
      ? ServerStatus.fromPartial(object.status)
      : undefined;
    message.tools = object.tools?.map((e) => ToolInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseListToolsInput(): ListToolsInput {
  return { serverName: "", filter: "" };
}

export const ListToolsInput: MessageFns<ListToolsInput> = {
  encode(message: ListToolsInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverName !== "") {
      writer.uint32(10).string(message.serverName);
    }
    if (message.filter !== "") {
      writer.uint32(18).string(message.filter);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListToolsInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListToolsInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filter = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ListToolsInput>, I>>(base?: I): ListToolsInput {
    return ListToolsInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListToolsInput>, I>>(object: I): ListToolsInput {
    const message = createBaseListToolsInput();
    message.serverName = object.serverName ?? "";
    message.filter = object.filter ?? "";
    return message;
  },
};

function createBaseListToolsOutput(): ListToolsOutput {
  return { tools: [], totalCount: 0 };
}

export const ListToolsOutput: MessageFns<ListToolsOutput> = {
  encode(message: ListToolsOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tools) {
      ToolInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.totalCount !== 0) {
      writer.uint32(16).int32(message.totalCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListToolsOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListToolsOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tools.push(ToolInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ListToolsOutput>, I>>(base?: I): ListToolsOutput {
    return ListToolsOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListToolsOutput>, I>>(object: I): ListToolsOutput {
    const message = createBaseListToolsOutput();
    message.tools = object.tools?.map((e) => ToolInfo.fromPartial(e)) || [];
    message.totalCount = object.totalCount ?? 0;
    return message;
  },
};

function createBaseSearchResult(): SearchResult {
  return { documentId: "", content: "", score: 0, fields: {} };
}

export const SearchResult: MessageFns<SearchResult> = {
  encode(message: SearchResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documentId !== "") {
      writer.uint32(10).string(message.documentId);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    if (message.score !== 0) {
      writer.uint32(29).float(message.score);
    }
    globalThis.Object.entries(message.fields).forEach(([key, value]: [string, string]) => {
      SearchResult_FieldsEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.documentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.score = reader.float();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = SearchResult_FieldsEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.fields[entry4.key] = entry4.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SearchResult>, I>>(base?: I): SearchResult {
    return SearchResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchResult>, I>>(object: I): SearchResult {
    const message = createBaseSearchResult();
    message.documentId = object.documentId ?? "";
    message.content = object.content ?? "";
    message.score = object.score ?? 0;
    message.fields = (globalThis.Object.entries(object.fields ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseSearchResult_FieldsEntry(): SearchResult_FieldsEntry {
  return { key: "", value: "" };
}

export const SearchResult_FieldsEntry: MessageFns<SearchResult_FieldsEntry> = {
  encode(message: SearchResult_FieldsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchResult_FieldsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchResult_FieldsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SearchResult_FieldsEntry>, I>>(base?: I): SearchResult_FieldsEntry {
    return SearchResult_FieldsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchResult_FieldsEntry>, I>>(object: I): SearchResult_FieldsEntry {
    const message = createBaseSearchResult_FieldsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseSearchToolsInput(): SearchToolsInput {
  return { query: "", indexName: "", topK: 0, semanticRatio: 0, scoreThreshold: 0, embedder: "" };
}

export const SearchToolsInput: MessageFns<SearchToolsInput> = {
  encode(message: SearchToolsInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    if (message.indexName !== "") {
      writer.uint32(18).string(message.indexName);
    }
    if (message.topK !== 0) {
      writer.uint32(24).int32(message.topK);
    }
    if (message.semanticRatio !== 0) {
      writer.uint32(37).float(message.semanticRatio);
    }
    if (message.scoreThreshold !== 0) {
      writer.uint32(45).float(message.scoreThreshold);
    }
    if (message.embedder !== "") {
      writer.uint32(50).string(message.embedder);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchToolsInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchToolsInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.indexName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.topK = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.semanticRatio = reader.float();
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.scoreThreshold = reader.float();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.embedder = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SearchToolsInput>, I>>(base?: I): SearchToolsInput {
    return SearchToolsInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchToolsInput>, I>>(object: I): SearchToolsInput {
    const message = createBaseSearchToolsInput();
    message.query = object.query ?? "";
    message.indexName = object.indexName ?? "";
    message.topK = object.topK ?? 0;
    message.semanticRatio = object.semanticRatio ?? 0;
    message.scoreThreshold = object.scoreThreshold ?? 0;
    message.embedder = object.embedder ?? "";
    return message;
  },
};

function createBaseSearchToolsOutput(): SearchToolsOutput {
  return { results: [], totalHits: 0, indexName: "" };
}

export const SearchToolsOutput: MessageFns<SearchToolsOutput> = {
  encode(message: SearchToolsOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.results) {
      SearchResult.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.totalHits !== 0) {
      writer.uint32(16).int32(message.totalHits);
    }
    if (message.indexName !== "") {
      writer.uint32(26).string(message.indexName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchToolsOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchToolsOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.results.push(SearchResult.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalHits = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.indexName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SearchToolsOutput>, I>>(base?: I): SearchToolsOutput {
    return SearchToolsOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchToolsOutput>, I>>(object: I): SearchToolsOutput {
    const message = createBaseSearchToolsOutput();
    message.results = object.results?.map((e) => SearchResult.fromPartial(e)) || [];
    message.totalHits = object.totalHits ?? 0;
    message.indexName = object.indexName ?? "";
    return message;
  },
};

function createBaseToolIndexDocument(): ToolIndexDocument {
  return { documentId: "", content: "", fields: {} };
}

export const ToolIndexDocument: MessageFns<ToolIndexDocument> = {
  encode(message: ToolIndexDocument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documentId !== "") {
      writer.uint32(10).string(message.documentId);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    globalThis.Object.entries(message.fields).forEach(([key, value]: [string, string]) => {
      ToolIndexDocument_FieldsEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolIndexDocument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolIndexDocument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.documentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = ToolIndexDocument_FieldsEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.fields[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ToolIndexDocument>, I>>(base?: I): ToolIndexDocument {
    return ToolIndexDocument.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolIndexDocument>, I>>(object: I): ToolIndexDocument {
    const message = createBaseToolIndexDocument();
    message.documentId = object.documentId ?? "";
    message.content = object.content ?? "";
    message.fields = (globalThis.Object.entries(object.fields ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseToolIndexDocument_FieldsEntry(): ToolIndexDocument_FieldsEntry {
  return { key: "", value: "" };
}

export const ToolIndexDocument_FieldsEntry: MessageFns<ToolIndexDocument_FieldsEntry> = {
  encode(message: ToolIndexDocument_FieldsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolIndexDocument_FieldsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolIndexDocument_FieldsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ToolIndexDocument_FieldsEntry>, I>>(base?: I): ToolIndexDocument_FieldsEntry {
    return ToolIndexDocument_FieldsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolIndexDocument_FieldsEntry>, I>>(
    object: I,
  ): ToolIndexDocument_FieldsEntry {
    const message = createBaseToolIndexDocument_FieldsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseIndexToolsInput(): IndexToolsInput {
  return { indexName: "", documents: [], action: "", enableVector: false, embedder: "" };
}

export const IndexToolsInput: MessageFns<IndexToolsInput> = {
  encode(message: IndexToolsInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.indexName !== "") {
      writer.uint32(10).string(message.indexName);
    }
    for (const v of message.documents) {
      ToolIndexDocument.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.action !== "") {
      writer.uint32(26).string(message.action);
    }
    if (message.enableVector !== false) {
      writer.uint32(32).bool(message.enableVector);
    }
    if (message.embedder !== "") {
      writer.uint32(42).string(message.embedder);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexToolsInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndexToolsInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.indexName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.documents.push(ToolIndexDocument.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.action = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.enableVector = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.embedder = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<IndexToolsInput>, I>>(base?: I): IndexToolsInput {
    return IndexToolsInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexToolsInput>, I>>(object: I): IndexToolsInput {
    const message = createBaseIndexToolsInput();
    message.indexName = object.indexName ?? "";
    message.documents = object.documents?.map((e) => ToolIndexDocument.fromPartial(e)) || [];
    message.action = object.action ?? "";
    message.enableVector = object.enableVector ?? false;
    message.embedder = object.embedder ?? "";
    return message;
  },
};

function createBaseIndexToolsOutput(): IndexToolsOutput {
  return { indexedCount: 0, indexed: [], failed: [] };
}

export const IndexToolsOutput: MessageFns<IndexToolsOutput> = {
  encode(message: IndexToolsOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.indexedCount !== 0) {
      writer.uint32(8).int32(message.indexedCount);
    }
    for (const v of message.indexed) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.failed) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexToolsOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndexToolsOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.indexedCount = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.indexed.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.failed.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<IndexToolsOutput>, I>>(base?: I): IndexToolsOutput {
    return IndexToolsOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexToolsOutput>, I>>(object: I): IndexToolsOutput {
    const message = createBaseIndexToolsOutput();
    message.indexedCount = object.indexedCount ?? 0;
    message.indexed = object.indexed?.map((e) => e) || [];
    message.failed = object.failed?.map((e) => e) || [];
    return message;
  },
};

function createBaseCallToolInput(): CallToolInput {
  return { request: undefined };
}

export const CallToolInput: MessageFns<CallToolInput> = {
  encode(message: CallToolInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.request !== undefined) {
      ToolCallRequest.encode(message.request, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CallToolInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCallToolInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.request = ToolCallRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CallToolInput>, I>>(base?: I): CallToolInput {
    return CallToolInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CallToolInput>, I>>(object: I): CallToolInput {
    const message = createBaseCallToolInput();
    message.request = (object.request !== undefined && object.request !== null)
      ? ToolCallRequest.fromPartial(object.request)
      : undefined;
    return message;
  },
};

function createBaseCallToolOutput(): CallToolOutput {
  return { response: undefined };
}

export const CallToolOutput: MessageFns<CallToolOutput> = {
  encode(message: CallToolOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.response !== undefined) {
      ToolCallResponse.encode(message.response, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CallToolOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCallToolOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.response = ToolCallResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<CallToolOutput>, I>>(base?: I): CallToolOutput {
    return CallToolOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CallToolOutput>, I>>(object: I): CallToolOutput {
    const message = createBaseCallToolOutput();
    message.response = (object.response !== undefined && object.response !== null)
      ? ToolCallResponse.fromPartial(object.response)
      : undefined;
    return message;
  },
};

function createBaseBatchStartInput(): BatchStartInput {
  return { names: [], timeoutMs: 0, parallel: false };
}

export const BatchStartInput: MessageFns<BatchStartInput> = {
  encode(message: BatchStartInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.names) {
      writer.uint32(10).string(v!);
    }
    if (message.timeoutMs !== 0) {
      writer.uint32(16).int32(message.timeoutMs);
    }
    if (message.parallel !== false) {
      writer.uint32(24).bool(message.parallel);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchStartInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchStartInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.names.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timeoutMs = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.parallel = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<BatchStartInput>, I>>(base?: I): BatchStartInput {
    return BatchStartInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchStartInput>, I>>(object: I): BatchStartInput {
    const message = createBaseBatchStartInput();
    message.names = object.names?.map((e) => e) || [];
    message.timeoutMs = object.timeoutMs ?? 0;
    message.parallel = object.parallel ?? false;
    return message;
  },
};

function createBaseBatchStartOutput(): BatchStartOutput {
  return { results: {}, failed: [] };
}

export const BatchStartOutput: MessageFns<BatchStartOutput> = {
  encode(message: BatchStartOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.results).forEach(([key, value]: [string, ServerStatus]) => {
      BatchStartOutput_ResultsEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    for (const v of message.failed) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchStartOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchStartOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = BatchStartOutput_ResultsEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.results[entry1.key] = entry1.value;
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.failed.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<BatchStartOutput>, I>>(base?: I): BatchStartOutput {
    return BatchStartOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchStartOutput>, I>>(object: I): BatchStartOutput {
    const message = createBaseBatchStartOutput();
    message.results = (globalThis.Object.entries(object.results ?? {}) as [string, ServerStatus][]).reduce(
      (acc: { [key: string]: ServerStatus }, [key, value]: [string, ServerStatus]) => {
        if (value !== undefined) {
          acc[key] = ServerStatus.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.failed = object.failed?.map((e) => e) || [];
    return message;
  },
};

function createBaseBatchStartOutput_ResultsEntry(): BatchStartOutput_ResultsEntry {
  return { key: "", value: undefined };
}

export const BatchStartOutput_ResultsEntry: MessageFns<BatchStartOutput_ResultsEntry> = {
  encode(message: BatchStartOutput_ResultsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      ServerStatus.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchStartOutput_ResultsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchStartOutput_ResultsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = ServerStatus.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<BatchStartOutput_ResultsEntry>, I>>(base?: I): BatchStartOutput_ResultsEntry {
    return BatchStartOutput_ResultsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchStartOutput_ResultsEntry>, I>>(
    object: I,
  ): BatchStartOutput_ResultsEntry {
    const message = createBaseBatchStartOutput_ResultsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? ServerStatus.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseBatchStopInput(): BatchStopInput {
  return { names: [], graceful: false };
}

export const BatchStopInput: MessageFns<BatchStopInput> = {
  encode(message: BatchStopInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.names) {
      writer.uint32(10).string(v!);
    }
    if (message.graceful !== false) {
      writer.uint32(16).bool(message.graceful);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchStopInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchStopInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.names.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.graceful = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<BatchStopInput>, I>>(base?: I): BatchStopInput {
    return BatchStopInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchStopInput>, I>>(object: I): BatchStopInput {
    const message = createBaseBatchStopInput();
    message.names = object.names?.map((e) => e) || [];
    message.graceful = object.graceful ?? false;
    return message;
  },
};

function createBaseBatchStopOutput(): BatchStopOutput {
  return { results: {}, failed: [] };
}

export const BatchStopOutput: MessageFns<BatchStopOutput> = {
  encode(message: BatchStopOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.results).forEach(([key, value]: [string, ServerStatus]) => {
      BatchStopOutput_ResultsEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    for (const v of message.failed) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchStopOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchStopOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = BatchStopOutput_ResultsEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.results[entry1.key] = entry1.value;
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.failed.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<BatchStopOutput>, I>>(base?: I): BatchStopOutput {
    return BatchStopOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchStopOutput>, I>>(object: I): BatchStopOutput {
    const message = createBaseBatchStopOutput();
    message.results = (globalThis.Object.entries(object.results ?? {}) as [string, ServerStatus][]).reduce(
      (acc: { [key: string]: ServerStatus }, [key, value]: [string, ServerStatus]) => {
        if (value !== undefined) {
          acc[key] = ServerStatus.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.failed = object.failed?.map((e) => e) || [];
    return message;
  },
};

function createBaseBatchStopOutput_ResultsEntry(): BatchStopOutput_ResultsEntry {
  return { key: "", value: undefined };
}

export const BatchStopOutput_ResultsEntry: MessageFns<BatchStopOutput_ResultsEntry> = {
  encode(message: BatchStopOutput_ResultsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      ServerStatus.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchStopOutput_ResultsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchStopOutput_ResultsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = ServerStatus.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<BatchStopOutput_ResultsEntry>, I>>(base?: I): BatchStopOutput_ResultsEntry {
    return BatchStopOutput_ResultsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchStopOutput_ResultsEntry>, I>>(object: I): BatchStopOutput_ResultsEntry {
    const message = createBaseBatchStopOutput_ResultsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? ServerStatus.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseServerEventInput(): ServerEventInput {
  return { eventType: "", status: undefined, message: "", timestamp: 0 };
}

export const ServerEventInput: MessageFns<ServerEventInput> = {
  encode(message: ServerEventInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventType !== "") {
      writer.uint32(10).string(message.eventType);
    }
    if (message.status !== undefined) {
      ServerStatus.encode(message.status, writer.uint32(18).fork()).join();
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    if (message.timestamp !== 0) {
      writer.uint32(32).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerEventInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerEventInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = ServerStatus.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ServerEventInput>, I>>(base?: I): ServerEventInput {
    return ServerEventInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerEventInput>, I>>(object: I): ServerEventInput {
    const message = createBaseServerEventInput();
    message.eventType = object.eventType ?? "";
    message.status = (object.status !== undefined && object.status !== null)
      ? ServerStatus.fromPartial(object.status)
      : undefined;
    message.message = object.message ?? "";
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseServerEventOutput(): ServerEventOutput {
  return { acknowledged: false };
}

export const ServerEventOutput: MessageFns<ServerEventOutput> = {
  encode(message: ServerEventOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.acknowledged !== false) {
      writer.uint32(8).bool(message.acknowledged);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerEventOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerEventOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.acknowledged = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ServerEventOutput>, I>>(base?: I): ServerEventOutput {
    return ServerEventOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerEventOutput>, I>>(object: I): ServerEventOutput {
    const message = createBaseServerEventOutput();
    message.acknowledged = object.acknowledged ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
