// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v6.33.6
// source: rag/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "lightrag.eventbus.topics.v1";

export interface KeywordExtractionInput {
  /** 用户原始查询 */
  query: string;
}

export interface KeywordExtractionOutput {
  /** 高层概念关键词 */
  hlKeywords: string[];
  /** 底层实体关键词 */
  llKeywords: string[];
}

export interface QueryExpansionInput {
  /** 高层关键词列表 */
  hlKeywords: string[];
  /** 底层关键词列表 */
  llKeywords: string[];
  /** 扩展配置 (可选) */
  expansionConfig?: ExpansionConfig | undefined;
}

export interface ExpansionConfig {
  /** 同义词扩展 */
  synonym: boolean;
  /** 近义词扩展 */
  nearSynonym: boolean;
  /** 术语库扩展 */
  terminology: boolean;
  /** 每个关键词最大扩展数 */
  maxExpansionPerKeyword: number;
}

export interface QueryExpansionOutput {
  /** 扩展后的高层关键词 */
  expandedHlKeywords: string[];
  /** 扩展后的底层关键词 */
  expandedLlKeywords: string[];
  /** 扩展元数据 (可选) */
  expansionMetadata?: ExpansionMetadata | undefined;
}

export interface ExpansionMetadata {
  /** 扩展来源 */
  source: string;
  /** 命中率 */
  hitRate: number;
}

export interface VectorSearchInput {
  /** 用户查询 */
  query: string;
  /** 返回 chunk 数量 (默认 20) */
  topK: number;
  /** 是否启用重排序 (默认 true) */
  enableRerank: boolean;
}

export interface VectorSearchOutput {
  /** 检索到的 chunk 列表 */
  chunks: VectorChunk[];
}

export interface VectorChunk {
  /** chunk 文本内容 */
  content: string;
  /** 创建时间戳 */
  createdAt: number;
  /** 来源文件路径 */
  filePath: string;
  /** 来源类型 (如 "vector") */
  sourceType: string;
  /** chunk ID */
  chunkId: string;
}

export interface KgSearchInput {
  /** 高层关键词 (global/hybrid/mix 模式) */
  hlKeywords: string[];
  /** 底层关键词 (local/hybrid/mix 模式) */
  llKeywords: string[];
  /** 查询模式 (默认 mix) */
  mode: string;
  /** 返回数量 (默认 60) */
  topK: number;
}

export interface KgSearchOutput {
  /** 实体列表 */
  entities: KgEntity[];
  /** 关系列表 */
  relations: KgRelation[];
}

export interface KgEntity {
  /** 实体 ID */
  entityId: string;
  /** 实体名称 */
  entityName: string;
  /** 实体类型 */
  entityType: string;
  /** 描述 */
  description: string;
  /** 节点度数 */
  rank: number;
  /** 来源 chunk ID */
  sourceId: string;
  /** 来源文件路径 */
  filePath: string;
  /** 创建时间戳 */
  createdAt: number;
  /** 截断信息 */
  truncate: string;
}

export interface KgRelation {
  /** 源实体 ID */
  srcId: string;
  /** 目标实体 ID */
  tgtId: string;
  /** 关系权重 */
  weight: number;
  /** 描述 */
  description: string;
  /** 关键词 */
  keywords: string;
  /** 边度数 */
  rank: number;
  /** 来源 chunk ID */
  sourceId: string;
  /** 来源文件路径 */
  filePath: string;
  /** 创建时间戳 */
  createdAt: number;
  /** 截断信息 */
  truncate: string;
}

export interface RerankInput {
  /** 用户查询 */
  query: string;
  /** 待重排的 chunk 列表 */
  chunks: RerankChunk[];
  /** 是否启用重排 (默认 true) */
  enableRerank: boolean;
}

export interface RerankChunk {
  /** chunk 文本内容 */
  content: string;
  /** 创建时间戳 */
  createdAt: number;
  /** 来源文件路径 */
  filePath: string;
  /** 来源类型 */
  sourceType: string;
  /** chunk ID */
  chunkId: string;
}

export interface RerankOutput {
  /** 重排后的 chunk 列表 */
  rankedChunks: RerankChunk[];
}

export interface ResponseInput {
  /** 用户查询 */
  query: string;
  /** 检索到的上下文 */
  context: string;
  /** 是否启用流式输出 (默认 false) */
  stream: boolean;
  /** 响应格式 */
  responseType: string;
  /** 对话历史 (可选，空列表表示无历史) */
  conversationHistory: ConversationTurn[];
  /** 附加用户指令 */
  userPrompt: string;
}

export interface ConversationTurn {
  /** "user" 或 "assistant" */
  role: string;
  /** 消息内容 */
  content: string;
}

export interface ResponseOutput {
  /** 生成的回答 */
  response: string;
}

function createBaseKeywordExtractionInput(): KeywordExtractionInput {
  return { query: "" };
}

export const KeywordExtractionInput: MessageFns<KeywordExtractionInput> = {
  encode(message: KeywordExtractionInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeywordExtractionInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeywordExtractionInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<KeywordExtractionInput>, I>>(base?: I): KeywordExtractionInput {
    return KeywordExtractionInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeywordExtractionInput>, I>>(object: I): KeywordExtractionInput {
    const message = createBaseKeywordExtractionInput();
    message.query = object.query ?? "";
    return message;
  },
};

function createBaseKeywordExtractionOutput(): KeywordExtractionOutput {
  return { hlKeywords: [], llKeywords: [] };
}

export const KeywordExtractionOutput: MessageFns<KeywordExtractionOutput> = {
  encode(message: KeywordExtractionOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.hlKeywords) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.llKeywords) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeywordExtractionOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeywordExtractionOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hlKeywords.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.llKeywords.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<KeywordExtractionOutput>, I>>(base?: I): KeywordExtractionOutput {
    return KeywordExtractionOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeywordExtractionOutput>, I>>(object: I): KeywordExtractionOutput {
    const message = createBaseKeywordExtractionOutput();
    message.hlKeywords = object.hlKeywords?.map((e) => e) || [];
    message.llKeywords = object.llKeywords?.map((e) => e) || [];
    return message;
  },
};

function createBaseQueryExpansionInput(): QueryExpansionInput {
  return { hlKeywords: [], llKeywords: [], expansionConfig: undefined };
}

export const QueryExpansionInput: MessageFns<QueryExpansionInput> = {
  encode(message: QueryExpansionInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.hlKeywords) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.llKeywords) {
      writer.uint32(18).string(v!);
    }
    if (message.expansionConfig !== undefined) {
      ExpansionConfig.encode(message.expansionConfig, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryExpansionInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryExpansionInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hlKeywords.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.llKeywords.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.expansionConfig = ExpansionConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<QueryExpansionInput>, I>>(base?: I): QueryExpansionInput {
    return QueryExpansionInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryExpansionInput>, I>>(object: I): QueryExpansionInput {
    const message = createBaseQueryExpansionInput();
    message.hlKeywords = object.hlKeywords?.map((e) => e) || [];
    message.llKeywords = object.llKeywords?.map((e) => e) || [];
    message.expansionConfig = (object.expansionConfig !== undefined && object.expansionConfig !== null)
      ? ExpansionConfig.fromPartial(object.expansionConfig)
      : undefined;
    return message;
  },
};

function createBaseExpansionConfig(): ExpansionConfig {
  return { synonym: false, nearSynonym: false, terminology: false, maxExpansionPerKeyword: 0 };
}

export const ExpansionConfig: MessageFns<ExpansionConfig> = {
  encode(message: ExpansionConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.synonym !== false) {
      writer.uint32(8).bool(message.synonym);
    }
    if (message.nearSynonym !== false) {
      writer.uint32(16).bool(message.nearSynonym);
    }
    if (message.terminology !== false) {
      writer.uint32(24).bool(message.terminology);
    }
    if (message.maxExpansionPerKeyword !== 0) {
      writer.uint32(32).int32(message.maxExpansionPerKeyword);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExpansionConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExpansionConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.synonym = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.nearSynonym = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.terminology = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maxExpansionPerKeyword = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ExpansionConfig>, I>>(base?: I): ExpansionConfig {
    return ExpansionConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExpansionConfig>, I>>(object: I): ExpansionConfig {
    const message = createBaseExpansionConfig();
    message.synonym = object.synonym ?? false;
    message.nearSynonym = object.nearSynonym ?? false;
    message.terminology = object.terminology ?? false;
    message.maxExpansionPerKeyword = object.maxExpansionPerKeyword ?? 0;
    return message;
  },
};

function createBaseQueryExpansionOutput(): QueryExpansionOutput {
  return { expandedHlKeywords: [], expandedLlKeywords: [], expansionMetadata: undefined };
}

export const QueryExpansionOutput: MessageFns<QueryExpansionOutput> = {
  encode(message: QueryExpansionOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.expandedHlKeywords) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.expandedLlKeywords) {
      writer.uint32(18).string(v!);
    }
    if (message.expansionMetadata !== undefined) {
      ExpansionMetadata.encode(message.expansionMetadata, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryExpansionOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryExpansionOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.expandedHlKeywords.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.expandedLlKeywords.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.expansionMetadata = ExpansionMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<QueryExpansionOutput>, I>>(base?: I): QueryExpansionOutput {
    return QueryExpansionOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryExpansionOutput>, I>>(object: I): QueryExpansionOutput {
    const message = createBaseQueryExpansionOutput();
    message.expandedHlKeywords = object.expandedHlKeywords?.map((e) => e) || [];
    message.expandedLlKeywords = object.expandedLlKeywords?.map((e) => e) || [];
    message.expansionMetadata = (object.expansionMetadata !== undefined && object.expansionMetadata !== null)
      ? ExpansionMetadata.fromPartial(object.expansionMetadata)
      : undefined;
    return message;
  },
};

function createBaseExpansionMetadata(): ExpansionMetadata {
  return { source: "", hitRate: 0 };
}

export const ExpansionMetadata: MessageFns<ExpansionMetadata> = {
  encode(message: ExpansionMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== "") {
      writer.uint32(10).string(message.source);
    }
    if (message.hitRate !== 0) {
      writer.uint32(21).float(message.hitRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExpansionMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExpansionMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.hitRate = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ExpansionMetadata>, I>>(base?: I): ExpansionMetadata {
    return ExpansionMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExpansionMetadata>, I>>(object: I): ExpansionMetadata {
    const message = createBaseExpansionMetadata();
    message.source = object.source ?? "";
    message.hitRate = object.hitRate ?? 0;
    return message;
  },
};

function createBaseVectorSearchInput(): VectorSearchInput {
  return { query: "", topK: 0, enableRerank: false };
}

export const VectorSearchInput: MessageFns<VectorSearchInput> = {
  encode(message: VectorSearchInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    if (message.topK !== 0) {
      writer.uint32(16).int32(message.topK);
    }
    if (message.enableRerank !== false) {
      writer.uint32(24).bool(message.enableRerank);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VectorSearchInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVectorSearchInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.topK = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.enableRerank = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<VectorSearchInput>, I>>(base?: I): VectorSearchInput {
    return VectorSearchInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VectorSearchInput>, I>>(object: I): VectorSearchInput {
    const message = createBaseVectorSearchInput();
    message.query = object.query ?? "";
    message.topK = object.topK ?? 0;
    message.enableRerank = object.enableRerank ?? false;
    return message;
  },
};

function createBaseVectorSearchOutput(): VectorSearchOutput {
  return { chunks: [] };
}

export const VectorSearchOutput: MessageFns<VectorSearchOutput> = {
  encode(message: VectorSearchOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.chunks) {
      VectorChunk.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VectorSearchOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVectorSearchOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.chunks.push(VectorChunk.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<VectorSearchOutput>, I>>(base?: I): VectorSearchOutput {
    return VectorSearchOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VectorSearchOutput>, I>>(object: I): VectorSearchOutput {
    const message = createBaseVectorSearchOutput();
    message.chunks = object.chunks?.map((e) => VectorChunk.fromPartial(e)) || [];
    return message;
  },
};

function createBaseVectorChunk(): VectorChunk {
  return { content: "", createdAt: 0, filePath: "", sourceType: "", chunkId: "" };
}

export const VectorChunk: MessageFns<VectorChunk> = {
  encode(message: VectorChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.content !== "") {
      writer.uint32(10).string(message.content);
    }
    if (message.createdAt !== 0) {
      writer.uint32(16).int64(message.createdAt);
    }
    if (message.filePath !== "") {
      writer.uint32(26).string(message.filePath);
    }
    if (message.sourceType !== "") {
      writer.uint32(34).string(message.sourceType);
    }
    if (message.chunkId !== "") {
      writer.uint32(42).string(message.chunkId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VectorChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVectorChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.createdAt = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.filePath = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sourceType = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.chunkId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<VectorChunk>, I>>(base?: I): VectorChunk {
    return VectorChunk.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VectorChunk>, I>>(object: I): VectorChunk {
    const message = createBaseVectorChunk();
    message.content = object.content ?? "";
    message.createdAt = object.createdAt ?? 0;
    message.filePath = object.filePath ?? "";
    message.sourceType = object.sourceType ?? "";
    message.chunkId = object.chunkId ?? "";
    return message;
  },
};

function createBaseKgSearchInput(): KgSearchInput {
  return { hlKeywords: [], llKeywords: [], mode: "", topK: 0 };
}

export const KgSearchInput: MessageFns<KgSearchInput> = {
  encode(message: KgSearchInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.hlKeywords) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.llKeywords) {
      writer.uint32(18).string(v!);
    }
    if (message.mode !== "") {
      writer.uint32(26).string(message.mode);
    }
    if (message.topK !== 0) {
      writer.uint32(32).int32(message.topK);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KgSearchInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKgSearchInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hlKeywords.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.llKeywords.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.mode = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.topK = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<KgSearchInput>, I>>(base?: I): KgSearchInput {
    return KgSearchInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KgSearchInput>, I>>(object: I): KgSearchInput {
    const message = createBaseKgSearchInput();
    message.hlKeywords = object.hlKeywords?.map((e) => e) || [];
    message.llKeywords = object.llKeywords?.map((e) => e) || [];
    message.mode = object.mode ?? "";
    message.topK = object.topK ?? 0;
    return message;
  },
};

function createBaseKgSearchOutput(): KgSearchOutput {
  return { entities: [], relations: [] };
}

export const KgSearchOutput: MessageFns<KgSearchOutput> = {
  encode(message: KgSearchOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entities) {
      KgEntity.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.relations) {
      KgRelation.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KgSearchOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKgSearchOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entities.push(KgEntity.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.relations.push(KgRelation.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<KgSearchOutput>, I>>(base?: I): KgSearchOutput {
    return KgSearchOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KgSearchOutput>, I>>(object: I): KgSearchOutput {
    const message = createBaseKgSearchOutput();
    message.entities = object.entities?.map((e) => KgEntity.fromPartial(e)) || [];
    message.relations = object.relations?.map((e) => KgRelation.fromPartial(e)) || [];
    return message;
  },
};

function createBaseKgEntity(): KgEntity {
  return {
    entityId: "",
    entityName: "",
    entityType: "",
    description: "",
    rank: 0,
    sourceId: "",
    filePath: "",
    createdAt: 0,
    truncate: "",
  };
}

export const KgEntity: MessageFns<KgEntity> = {
  encode(message: KgEntity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.entityId !== "") {
      writer.uint32(10).string(message.entityId);
    }
    if (message.entityName !== "") {
      writer.uint32(18).string(message.entityName);
    }
    if (message.entityType !== "") {
      writer.uint32(26).string(message.entityType);
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    if (message.rank !== 0) {
      writer.uint32(40).int32(message.rank);
    }
    if (message.sourceId !== "") {
      writer.uint32(50).string(message.sourceId);
    }
    if (message.filePath !== "") {
      writer.uint32(58).string(message.filePath);
    }
    if (message.createdAt !== 0) {
      writer.uint32(64).int64(message.createdAt);
    }
    if (message.truncate !== "") {
      writer.uint32(74).string(message.truncate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KgEntity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKgEntity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entityId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.entityName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.entityType = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.rank = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.sourceId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.filePath = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.createdAt = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.truncate = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<KgEntity>, I>>(base?: I): KgEntity {
    return KgEntity.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KgEntity>, I>>(object: I): KgEntity {
    const message = createBaseKgEntity();
    message.entityId = object.entityId ?? "";
    message.entityName = object.entityName ?? "";
    message.entityType = object.entityType ?? "";
    message.description = object.description ?? "";
    message.rank = object.rank ?? 0;
    message.sourceId = object.sourceId ?? "";
    message.filePath = object.filePath ?? "";
    message.createdAt = object.createdAt ?? 0;
    message.truncate = object.truncate ?? "";
    return message;
  },
};

function createBaseKgRelation(): KgRelation {
  return {
    srcId: "",
    tgtId: "",
    weight: 0,
    description: "",
    keywords: "",
    rank: 0,
    sourceId: "",
    filePath: "",
    createdAt: 0,
    truncate: "",
  };
}

export const KgRelation: MessageFns<KgRelation> = {
  encode(message: KgRelation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.srcId !== "") {
      writer.uint32(10).string(message.srcId);
    }
    if (message.tgtId !== "") {
      writer.uint32(18).string(message.tgtId);
    }
    if (message.weight !== 0) {
      writer.uint32(29).float(message.weight);
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    if (message.keywords !== "") {
      writer.uint32(42).string(message.keywords);
    }
    if (message.rank !== 0) {
      writer.uint32(48).int32(message.rank);
    }
    if (message.sourceId !== "") {
      writer.uint32(58).string(message.sourceId);
    }
    if (message.filePath !== "") {
      writer.uint32(66).string(message.filePath);
    }
    if (message.createdAt !== 0) {
      writer.uint32(72).int64(message.createdAt);
    }
    if (message.truncate !== "") {
      writer.uint32(82).string(message.truncate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KgRelation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKgRelation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.srcId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tgtId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.weight = reader.float();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.keywords = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.rank = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.sourceId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.filePath = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.createdAt = longToNumber(reader.int64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.truncate = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<KgRelation>, I>>(base?: I): KgRelation {
    return KgRelation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KgRelation>, I>>(object: I): KgRelation {
    const message = createBaseKgRelation();
    message.srcId = object.srcId ?? "";
    message.tgtId = object.tgtId ?? "";
    message.weight = object.weight ?? 0;
    message.description = object.description ?? "";
    message.keywords = object.keywords ?? "";
    message.rank = object.rank ?? 0;
    message.sourceId = object.sourceId ?? "";
    message.filePath = object.filePath ?? "";
    message.createdAt = object.createdAt ?? 0;
    message.truncate = object.truncate ?? "";
    return message;
  },
};

function createBaseRerankInput(): RerankInput {
  return { query: "", chunks: [], enableRerank: false };
}

export const RerankInput: MessageFns<RerankInput> = {
  encode(message: RerankInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    for (const v of message.chunks) {
      RerankChunk.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.enableRerank !== false) {
      writer.uint32(24).bool(message.enableRerank);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RerankInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRerankInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chunks.push(RerankChunk.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.enableRerank = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RerankInput>, I>>(base?: I): RerankInput {
    return RerankInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RerankInput>, I>>(object: I): RerankInput {
    const message = createBaseRerankInput();
    message.query = object.query ?? "";
    message.chunks = object.chunks?.map((e) => RerankChunk.fromPartial(e)) || [];
    message.enableRerank = object.enableRerank ?? false;
    return message;
  },
};

function createBaseRerankChunk(): RerankChunk {
  return { content: "", createdAt: 0, filePath: "", sourceType: "", chunkId: "" };
}

export const RerankChunk: MessageFns<RerankChunk> = {
  encode(message: RerankChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.content !== "") {
      writer.uint32(10).string(message.content);
    }
    if (message.createdAt !== 0) {
      writer.uint32(16).int64(message.createdAt);
    }
    if (message.filePath !== "") {
      writer.uint32(26).string(message.filePath);
    }
    if (message.sourceType !== "") {
      writer.uint32(34).string(message.sourceType);
    }
    if (message.chunkId !== "") {
      writer.uint32(42).string(message.chunkId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RerankChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRerankChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.createdAt = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.filePath = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sourceType = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.chunkId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RerankChunk>, I>>(base?: I): RerankChunk {
    return RerankChunk.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RerankChunk>, I>>(object: I): RerankChunk {
    const message = createBaseRerankChunk();
    message.content = object.content ?? "";
    message.createdAt = object.createdAt ?? 0;
    message.filePath = object.filePath ?? "";
    message.sourceType = object.sourceType ?? "";
    message.chunkId = object.chunkId ?? "";
    return message;
  },
};

function createBaseRerankOutput(): RerankOutput {
  return { rankedChunks: [] };
}

export const RerankOutput: MessageFns<RerankOutput> = {
  encode(message: RerankOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.rankedChunks) {
      RerankChunk.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RerankOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRerankOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rankedChunks.push(RerankChunk.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RerankOutput>, I>>(base?: I): RerankOutput {
    return RerankOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RerankOutput>, I>>(object: I): RerankOutput {
    const message = createBaseRerankOutput();
    message.rankedChunks = object.rankedChunks?.map((e) => RerankChunk.fromPartial(e)) || [];
    return message;
  },
};

function createBaseResponseInput(): ResponseInput {
  return { query: "", context: "", stream: false, responseType: "", conversationHistory: [], userPrompt: "" };
}

export const ResponseInput: MessageFns<ResponseInput> = {
  encode(message: ResponseInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    if (message.context !== "") {
      writer.uint32(18).string(message.context);
    }
    if (message.stream !== false) {
      writer.uint32(24).bool(message.stream);
    }
    if (message.responseType !== "") {
      writer.uint32(34).string(message.responseType);
    }
    for (const v of message.conversationHistory) {
      ConversationTurn.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.userPrompt !== "") {
      writer.uint32(50).string(message.userPrompt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponseInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.context = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.stream = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.responseType = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.conversationHistory.push(ConversationTurn.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.userPrompt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResponseInput>, I>>(base?: I): ResponseInput {
    return ResponseInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseInput>, I>>(object: I): ResponseInput {
    const message = createBaseResponseInput();
    message.query = object.query ?? "";
    message.context = object.context ?? "";
    message.stream = object.stream ?? false;
    message.responseType = object.responseType ?? "";
    message.conversationHistory = object.conversationHistory?.map((e) => ConversationTurn.fromPartial(e)) || [];
    message.userPrompt = object.userPrompt ?? "";
    return message;
  },
};

function createBaseConversationTurn(): ConversationTurn {
  return { role: "", content: "" };
}

export const ConversationTurn: MessageFns<ConversationTurn> = {
  encode(message: ConversationTurn, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.role !== "") {
      writer.uint32(10).string(message.role);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConversationTurn {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConversationTurn();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.role = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ConversationTurn>, I>>(base?: I): ConversationTurn {
    return ConversationTurn.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConversationTurn>, I>>(object: I): ConversationTurn {
    const message = createBaseConversationTurn();
    message.role = object.role ?? "";
    message.content = object.content ?? "";
    return message;
  },
};

function createBaseResponseOutput(): ResponseOutput {
  return { response: "" };
}

export const ResponseOutput: MessageFns<ResponseOutput> = {
  encode(message: ResponseOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.response !== "") {
      writer.uint32(10).string(message.response);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResponseOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponseOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.response = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ResponseOutput>, I>>(base?: I): ResponseOutput {
    return ResponseOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResponseOutput>, I>>(object: I): ResponseOutput {
    const message = createBaseResponseOutput();
    message.response = object.response ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
