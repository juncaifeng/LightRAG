// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v6.33.6
// source: index/builder.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { EmbeddingVector } from "../embedding/embed";

export const protobufPackage = "lightrag.eventbus.topics.v1";

export enum IndexBuildAction {
  INDEX_BUILD_ACTION_UNSPECIFIED = 0,
  /** INDEX_BUILD_ACTION_UPSERT - 插入或更新 */
  INDEX_BUILD_ACTION_UPSERT = 1,
  /** INDEX_BUILD_ACTION_DELETE - 按 document_id 删除 */
  INDEX_BUILD_ACTION_DELETE = 2,
  /** INDEX_BUILD_ACTION_REBUILD - 清空索引后重建（慎用） */
  INDEX_BUILD_ACTION_REBUILD = 3,
  UNRECOGNIZED = -1,
}

export interface IndexDocument {
  /** 文档唯一标识（需符合索引引擎的 ID 规则） */
  documentId: string;
  /** 原始文本，供索引服务自动向量化（可选） */
  content: string;
  /** 结构化字段：key=字段名, value=JSON 字符串或纯文本 */
  fields: { [key: string]: string };
  /** 预计算向量：key=embedder 名称（如 "default"） */
  vectors: { [key: string]: EmbeddingVector };
}

export interface IndexDocument_FieldsEntry {
  key: string;
  value: string;
}

export interface IndexDocument_VectorsEntry {
  key: string;
  value: EmbeddingVector | undefined;
}

export interface IndexBuildInput {
  /** 目标索引名称，如 "thesaurus" */
  indexName: string;
  /** 操作类型 */
  action: IndexBuildAction;
  /** 文档批次 */
  documents: IndexDocument[];
  /** 索引级配置覆盖（如 searchableAttributes, embedder 参数等） */
  config: { [key: string]: string };
}

export interface IndexBuildInput_ConfigEntry {
  key: string;
  value: string;
}

export interface IndexBuildOutput {
  indexName: string;
  successCount: number;
  failedCount: number;
  failedDocumentIds: string[];
  /** 错误摘要或成功提示 */
  message: string;
}

function createBaseIndexDocument(): IndexDocument {
  return { documentId: "", content: "", fields: {}, vectors: {} };
}

export const IndexDocument: MessageFns<IndexDocument> = {
  encode(message: IndexDocument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.documentId !== "") {
      writer.uint32(10).string(message.documentId);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    globalThis.Object.entries(message.fields).forEach(([key, value]: [string, string]) => {
      IndexDocument_FieldsEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    globalThis.Object.entries(message.vectors).forEach(([key, value]: [string, EmbeddingVector]) => {
      IndexDocument_VectorsEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexDocument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndexDocument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.documentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = IndexDocument_FieldsEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.fields[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = IndexDocument_VectorsEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.vectors[entry4.key] = entry4.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<IndexDocument>, I>>(base?: I): IndexDocument {
    return IndexDocument.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexDocument>, I>>(object: I): IndexDocument {
    const message = createBaseIndexDocument();
    message.documentId = object.documentId ?? "";
    message.content = object.content ?? "";
    message.fields = (globalThis.Object.entries(object.fields ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.vectors = (globalThis.Object.entries(object.vectors ?? {}) as [string, EmbeddingVector][]).reduce(
      (acc: { [key: string]: EmbeddingVector }, [key, value]: [string, EmbeddingVector]) => {
        if (value !== undefined) {
          acc[key] = EmbeddingVector.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseIndexDocument_FieldsEntry(): IndexDocument_FieldsEntry {
  return { key: "", value: "" };
}

export const IndexDocument_FieldsEntry: MessageFns<IndexDocument_FieldsEntry> = {
  encode(message: IndexDocument_FieldsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexDocument_FieldsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndexDocument_FieldsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<IndexDocument_FieldsEntry>, I>>(base?: I): IndexDocument_FieldsEntry {
    return IndexDocument_FieldsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexDocument_FieldsEntry>, I>>(object: I): IndexDocument_FieldsEntry {
    const message = createBaseIndexDocument_FieldsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseIndexDocument_VectorsEntry(): IndexDocument_VectorsEntry {
  return { key: "", value: undefined };
}

export const IndexDocument_VectorsEntry: MessageFns<IndexDocument_VectorsEntry> = {
  encode(message: IndexDocument_VectorsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      EmbeddingVector.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexDocument_VectorsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndexDocument_VectorsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = EmbeddingVector.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<IndexDocument_VectorsEntry>, I>>(base?: I): IndexDocument_VectorsEntry {
    return IndexDocument_VectorsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexDocument_VectorsEntry>, I>>(object: I): IndexDocument_VectorsEntry {
    const message = createBaseIndexDocument_VectorsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? EmbeddingVector.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseIndexBuildInput(): IndexBuildInput {
  return { indexName: "", action: 0, documents: [], config: {} };
}

export const IndexBuildInput: MessageFns<IndexBuildInput> = {
  encode(message: IndexBuildInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.indexName !== "") {
      writer.uint32(10).string(message.indexName);
    }
    if (message.action !== 0) {
      writer.uint32(16).int32(message.action);
    }
    for (const v of message.documents) {
      IndexDocument.encode(v!, writer.uint32(26).fork()).join();
    }
    globalThis.Object.entries(message.config).forEach(([key, value]: [string, string]) => {
      IndexBuildInput_ConfigEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexBuildInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndexBuildInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.indexName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.action = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.documents.push(IndexDocument.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = IndexBuildInput_ConfigEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.config[entry4.key] = entry4.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<IndexBuildInput>, I>>(base?: I): IndexBuildInput {
    return IndexBuildInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexBuildInput>, I>>(object: I): IndexBuildInput {
    const message = createBaseIndexBuildInput();
    message.indexName = object.indexName ?? "";
    message.action = object.action ?? 0;
    message.documents = object.documents?.map((e) => IndexDocument.fromPartial(e)) || [];
    message.config = (globalThis.Object.entries(object.config ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseIndexBuildInput_ConfigEntry(): IndexBuildInput_ConfigEntry {
  return { key: "", value: "" };
}

export const IndexBuildInput_ConfigEntry: MessageFns<IndexBuildInput_ConfigEntry> = {
  encode(message: IndexBuildInput_ConfigEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexBuildInput_ConfigEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndexBuildInput_ConfigEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<IndexBuildInput_ConfigEntry>, I>>(base?: I): IndexBuildInput_ConfigEntry {
    return IndexBuildInput_ConfigEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexBuildInput_ConfigEntry>, I>>(object: I): IndexBuildInput_ConfigEntry {
    const message = createBaseIndexBuildInput_ConfigEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseIndexBuildOutput(): IndexBuildOutput {
  return { indexName: "", successCount: 0, failedCount: 0, failedDocumentIds: [], message: "" };
}

export const IndexBuildOutput: MessageFns<IndexBuildOutput> = {
  encode(message: IndexBuildOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.indexName !== "") {
      writer.uint32(10).string(message.indexName);
    }
    if (message.successCount !== 0) {
      writer.uint32(16).int32(message.successCount);
    }
    if (message.failedCount !== 0) {
      writer.uint32(24).int32(message.failedCount);
    }
    for (const v of message.failedDocumentIds) {
      writer.uint32(34).string(v!);
    }
    if (message.message !== "") {
      writer.uint32(42).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexBuildOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndexBuildOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.indexName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.successCount = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.failedCount = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.failedDocumentIds.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<IndexBuildOutput>, I>>(base?: I): IndexBuildOutput {
    return IndexBuildOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexBuildOutput>, I>>(object: I): IndexBuildOutput {
    const message = createBaseIndexBuildOutput();
    message.indexName = object.indexName ?? "";
    message.successCount = object.successCount ?? 0;
    message.failedCount = object.failedCount ?? 0;
    message.failedDocumentIds = object.failedDocumentIds?.map((e) => e) || [];
    message.message = object.message ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
